# ===========================================
# Clear Dues Backend - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# Server
PORT=3000
NODE_ENV=development

# MongoDB
# Local: mongodb://localhost:27017/cleardues
# Atlas: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/cleardues
MONGODB_URI=mongodb://localhost:27017/cleardues

# JWT Authentication
# IMPORTANT: Generate a secure secret for production!
# Run: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS (comma-separated list for production)
ALLOWED_ORIGINS=http://localhost:3000

# Rate Limiting (requests per window)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
